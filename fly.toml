# fly.toml app configuration file generated for mollysocket-fragrant-water-7601 on 2023-12-29T18:50:52+01:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = "mollysocket-fragrant-water-7601"
primary_region = "arn"

[build]
  dockerfile = "Dockerfile"

[processes]
  worker = "server"

[[mounts]]
  source = "mollysocket_data"
  destination = "/app"
  initial_size = "3GB"
  auto_extend_size_threshold = 0

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
  processes = ["worker"]
